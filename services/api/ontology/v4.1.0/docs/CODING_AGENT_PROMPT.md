# Coding Agent Prompt — Integrate AceCoach v4\n\nImplement the v4 ontology as the source of truth. Do not shortcut directly from pose keypoints to player prose. Build the pipeline in the order defined in `config/video_analysis_protocol.json`. Persist event intervals, metric time series, context clusters, causal hypotheses and visual-story payloads. Render player video from `visual_story_compiler.json`; do not hardcode UI coaching rules. Ensure every player-facing claim resolves to `evidence_ids` and a comparison basis. LLM usage is limited to language realization after structured insight selection.\n\nAcceptance tests: same source/config versions -> same structured output; low-quality views suppress unsupported metrics; self-best EMD can outrank visually dramatic symptoms; visual story contains one focus insight; claim traceability rejects unsupported prose.\n